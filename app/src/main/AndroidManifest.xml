<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gesturedemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DragDropGestureActivity"
            android:label="@string/title_activity_pan_gesture"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PinchZoomGestureActivity"
            android:label="@string/title_activity_resize_gesture"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RotateGestureActivity"
            android:label="@string/title_activity_rotate_gesture"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>